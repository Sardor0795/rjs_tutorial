/*

    npx create-react-app no_big_letters -> no: react, react-dom, react-scripts, . just there (not open new folder)
    pacage.json, src, public
    spa -> single page application
    React Virtual DOM - uses diffing algorithm

    JSX -> JavaScript XML - syntax sugar of create element
    Browser dont reat it, Babel.js converts it browser readable file

    pwd
    ls
    cd / cd ..
    let root = ReactDOM.createRoot(document.getElementById('root))
    root.render()

    let title = 'Hi React'
    let title = <p>Hi React</p>

    import './'
    import Smth, { Smth } from './

    Our all works in src file
    Git ignore file - ignores files which shouldn't upload

*/